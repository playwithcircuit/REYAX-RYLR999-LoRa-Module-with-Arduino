# REYAX-RYLR999-LoRa-Module-with-Arduino
This project demonstrates how to establish reliable communication between two Arduino boards using the RYLR999 LoRa transceiver. One Arduino acts as the **Initiator**, periodically transmitting a message, while the second Arduino functions as the Responder, receives the packet, and sends an acknowledgement back.
